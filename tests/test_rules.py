"""Tests for deterministic matching rules (R1–R5)."""

import pytest

# TODO: Implement tests per §13
# - One test per rule
# - Explicit must-not-fire cases for each
# - Required coverage:
#   * R3 with unexplainable delta
#   * R4 with two valid subsets
#   * R2 with two equally-good counterparty candidates
