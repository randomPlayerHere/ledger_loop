[project]
name = "ledger-loop"
version = "0.1.0"
description = "Autonomous bank-statement to ledger reconciliation agent"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.13.5",
    "rapidfuzz>=3.14.6",
    "pyyaml>=6.0",
]

[dependency-groups]
dev = [
    "pytest>=7.0",
]
